name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-14
    steps:
      - run: python3 -m unittest discover -s tests -v
      - run: python3 scripts/public_release_audit.py
