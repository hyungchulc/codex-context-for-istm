{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "codex-istm ISTM to Daily candidate",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "stage",
    "packet_sha256",
    "producer",
    "entries",
    "omitted"
  ],
  "properties": {
    "schema_version": {
      "const": "codex-istm-model-result-v1"
    },
    "stage": {
      "const": "istm_to_daily"
    },
    "packet_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "producer": {
      "$ref": "#/$defs/producer"
    },
    "entries": {
      "type": "array",
      "maxItems": 40,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "source_record_ids",
          "summary"
        ],
        "properties": {
          "source_record_ids": {
            "type": "array",
            "minItems": 1,
            "maxItems": 12,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "pattern": "^[0-9a-f]{64}$"
            }
          },
          "summary": {
            "type": "string",
            "minLength": 1,
            "maxLength": 1200
          }
        }
      }
    },
    "omitted": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "record_id",
          "reason"
        ],
        "properties": {
          "record_id": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$"
          },
          "reason": {
            "enum": [
              "low_signal",
              "redundant",
              "sensitive",
              "unsupported"
            ]
          }
        }
      }
    }
  },
  "$defs": {
    "producer": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "codex_cli_version",
        "model",
        "reasoning_effort",
        "isolation_profile"
      ],
      "properties": {
        "kind": {
          "const": "codex_cli"
        },
        "codex_cli_version": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "model": {
          "type": "string",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,99}$"
        },
        "reasoning_effort": {
          "enum": [
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            "ultra"
          ]
        },
        "isolation_profile": {
          "const": "codex-cli-no-tools-v1"
        }
      }
    }
  }
}
